clusterId: doc4
account: { id: doc4 }        # per-env account → ArgoCD globs only doc4/* in the config repo
storageClass: isilon
vault: { host: vault.apps.doc4.lac1.biz }
platform:  { repo_url: "https://gitlab.lac1.biz/gitops/platform-gitops.git", revision: "mas-vault-deploy" }
source:    { repo_url: "https://gitlab.lac1.biz/gitops/ibm-mas-gitops.git", revision: "8.4.0-vault-patch" }
generator: { repo_url: "https://gitlab.lac1.biz/gitops/mas-gitops-config.git", revision: "mas-vault-deploy" }
