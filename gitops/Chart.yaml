apiVersion: v2
name: gitops
description: Generates platform operators, MongoDB, the IBM MAS account root, automatic generated-secret publishing, and optional Grafana.
  Installation follows ordered operator, database, and MAS steps.
type: application
version: 1.0.0
