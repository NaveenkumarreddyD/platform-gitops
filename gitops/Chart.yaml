apiVersion: v2
name: gitops
description: Generates Vault, MongoDB, the IBM MAS account root, and optional Grafana.
  Installation is promoted through secrets, database, and mas stages.
type: application
version: 1.0.0
