# wave 50 — runtime registration sync (local chart). Manual by default because SLS/DRO
# are created only after the manual ibm-mas-account-root sync.
{{- if and (not .Values.rootOnly) .Values.enable.registrationSync .Values.sls.syncEnabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-sync-sls-{{ .Values.instanceId }}
  namespace: {{ .Values.argoNamespace }}
  labels: { app.kubernetes.io/part-of: mas-platform }
  annotations: { argocd.argoproj.io/sync-wave: "50" }
  finalizers: [ resources-finalizer.argocd.argoproj.io ]
spec:
  revisionHistoryLimit: 10
  project: {{ .Values.project }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ .Values.argoNamespace }}
  source:
    repoURL: {{ .Values.platform.repo_url | quote }}
    targetRevision: {{ .Values.platform.revision | quote }}
    path: workloads/vault-sync
    helm:
      values: |
        accountId: {{ .Values.account.id }}
        clusterId: {{ .Values.clusterId }}
        instanceId: {{ .Values.instanceId }}
        vaultAddr: {{ .Values.vault.addr }}
        vaultRole: {{ .Values.vault.writerRole }}
        namespace: {{ .Values.argoNamespace }}
        serviceAccount: vault-registration-sync
        deployShared: false
        ocImage: {{ .Values.images.oc }}
        vaultImage: {{ .Values.images.vault }}
        slsSync: { enabled: true, namespace: {{ include "gitops.slsNs" . | quote }} }
        droSync: { enabled: false, namespace: {{ .Values.dro.namespace }} }
        mongoSync: { enabled: false, namespace: {{ include "gitops.mongoNs" . }} }
{{- if .Values.registrationSync.autoSync }}
  syncPolicy:
    automated: { prune: {{ .Values.auto_delete }}, selfHeal: true }
    retry:
      # generous: the RoleBindings target mas-*-sls/-core namespaces created LATER by the instance
      # cascade. If this app syncs first, the apply fails until those namespaces exist — keep
      # retrying (~hours) so it self-heals through a slow cascade instead of giving up.
      limit: 30
      backoff: { duration: 5s, factor: 2, maxDuration: 5m }
{{- else }}
  # Intentionally no automated syncPolicy. Run scripts/sync-runtime-registration.sh
  # after SLS is initialized.
{{- end }}
{{- end }}
{{- if and (not .Values.rootOnly) .Values.enable.registrationSync .Values.dro.syncEnabled }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vault-sync-dro-{{ .Values.instanceId }}
  namespace: {{ .Values.argoNamespace }}
  labels: { app.kubernetes.io/part-of: mas-platform }
  annotations: { argocd.argoproj.io/sync-wave: "50" }
  finalizers: [ resources-finalizer.argocd.argoproj.io ]
spec:
  revisionHistoryLimit: 10
  project: {{ .Values.project }}
  destination:
    server: "https://kubernetes.default.svc"
    namespace: {{ .Values.argoNamespace }}
  source:
    repoURL: {{ .Values.platform.repo_url | quote }}
    targetRevision: {{ .Values.platform.revision | quote }}
    path: workloads/vault-sync
    helm:
      values: |
        accountId: {{ .Values.account.id }}
        clusterId: {{ .Values.clusterId }}
        instanceId: {{ .Values.instanceId }}
        vaultAddr: {{ .Values.vault.addr }}
        vaultRole: {{ .Values.vault.writerRole }}
        namespace: {{ .Values.argoNamespace }}
        serviceAccount: vault-registration-sync
        deployShared: false
        ocImage: {{ .Values.images.oc }}
        vaultImage: {{ .Values.images.vault }}
        slsSync: { enabled: false, namespace: {{ include "gitops.slsNs" . | quote }} }
        droSync: { enabled: true, namespace: {{ .Values.dro.namespace }} }
        mongoSync: { enabled: false, namespace: {{ include "gitops.mongoNs" . }} }
{{- if .Values.registrationSync.autoSync }}
  syncPolicy:
    automated: { prune: {{ .Values.auto_delete }}, selfHeal: true }
    retry:
      # generous: the RoleBindings target mas-*-sls/-core namespaces created LATER by the instance
      # cascade. If this app syncs first, the apply fails until those namespaces exist — keep
      # retrying (~hours) so it self-heals through a slow cascade instead of giving up.
      limit: 30
      backoff: { duration: 5s, factor: 2, maxDuration: 5m }
{{- else }}
  # Intentionally no automated syncPolicy. Run scripts/sync-runtime-registration.sh
  # after DRO is initialized.
{{- end }}
{{- end }}
